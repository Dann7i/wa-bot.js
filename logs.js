export async function eksekusi(pesan, gokubot) {
  console.log(`pesan di terima: ${pesan}`);
  if (gokubot) {
    console.log(`pesan dikirim oleh id: ${gokubot.id}`);
    const waktuSekarang = new Date();
    console.log(`waktu pemrosesan ${waktuSekrang.toLocaleString()}`);
    //logic bot
  }
}
